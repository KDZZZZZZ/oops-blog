---
import SketchIcon from "./SketchIcon.astro";

interface Props {
  label?: string;
}

const { label = "切换主题" } = Astro.props;
---

<button class="icon-button" id="theme-button" type="button" aria-label={label} title={label}>
  <SketchIcon id="theme-icon" name="moon" ring />
</button>
